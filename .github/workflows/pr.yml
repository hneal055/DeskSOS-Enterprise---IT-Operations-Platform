# DeskSOS Enterprise

[![CI/CD Pipeline](https://github.com/hneal055/DESKSOS-Desktop-App-/actions/workflows/ci.yml/badge.svg)](https://github.com/hneal055/DESKSOS-Desktop-App-/actions)
[![Tests](https://img.shields.io/badge/tests-17%20passing-brightgreen)](./tests/)
[![Node.js Version](https://img.shields.io/badge/node-%3E%3D20.0.0-brightgreen)](https://nodejs.org/)
[![npm Version](https://img.shields.io/badge/npm-%3E%3D11.0.0-brightgreen)](https://www.npmjs.com/)

Comprehensive system monitoring, management, and deployment platform.

[API Documentation](./docs/API_REFERENCE.md) | [OpenAPI Spec](./openapi.yaml) | [Workflows](./.github/WORKFLOWS.md)

## Pull Request Checks

on:
  pull_request:
    branches: [ main, develop ]

jobs:
  pr-checks:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20.x'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run tests
        run: npm test
      
      - name: Check test coverage
        run: npm run test:coverage
      
      - name: Comment PR with results
        if: always()
        uses: actions/github-script@v6
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '✅ CI checks completed!\n\nTests: All passing\nCoverage: Generated'
            })